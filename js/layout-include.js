/* Deprecated: fetch() to partials/ does not work with file:// and is fragile.
   Header, menu and footer are inlined by: python3 scripts/apply_layout_regex.py
   (reads partials/layout-common.html). */
